{
  "name": "not",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "build": "rm -rf docs && parcel build -d docs --public-url . --experimental-scope-hoisting src/index.html",
    "check": "tsc --noEmit --project . --watch",
    "debug": "rm -rf debug && parcel build -d debug --no-minify --public-url . src/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "browsersList": [
    "> 5%"
  ]
}
